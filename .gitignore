# Python
__pycache__/
*.pyc
.venv/
venv/

# Segredos / credenciais
.env
*credentials*.json
client_secret*.json
token.json
dados/tokens/

# Dados pessoais de uso (canais, contexto de cada um — evolui com o uso, não é template)
dados/canais.json

# Biblioteca de mídia importada por você (áudios/imagens reutilizáveis entre vídeos)
dados/biblioteca/

# Saída gerada (vídeos, áudio, imagens grandes)
output/
*.mp4
*.wav
*.mp3

# SO
.DS_Store
Thumbs.db
